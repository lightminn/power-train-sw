# ═══════════════════════════════════════════════════════
# ZETIN Rocker-Bogie GPU 가속 — Python 의존성
# ═══════════════════════════════════════════════════════
#
# 설치 순서:
#   1. CUDA 드라이버 확인 (nvidia-smi)
#   2. JAX GPU 버전 설치
#   3. 나머지 패키지 설치
#
# ── 기본 패키지 ──────────────────────────────────────
numpy>=1.23
scipy>=1.9
matplotlib>=3.5

# ── JAX GPU (CUDA 12.x 기준) ────────────────────────
# RTX 3090 / A100 모두 CUDA 12 권장
#
# pip install 명령:
#   pip install jax[cuda12]
#
# 또는 특정 버전 고정:
#   pip install jax[cuda12]==0.4.35
#
# CUDA 11.x를 사용하는 경우:
#   pip install jax[cuda11_pip]
#
jax[cuda12]>=0.4.20
