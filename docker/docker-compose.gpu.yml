# NVIDIA GPU 활성화 오버레이 (x86_64). nvidia-container-toolkit 호스트에 설치되어 있어야 함.
# 사용 예:
#   docker compose -f docker/docker-compose.yml -f docker/docker-compose.gpu.yml up -d --build

services:
  powertrain:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
